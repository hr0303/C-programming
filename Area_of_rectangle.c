//Area of rectangle 

#include <stdio.h>

void main()
{
    float l,b,area;
    /* l= lenght of the rectangle
       b= width of the recatnagle */
    printf("Enter the length:  ");
    scanf("%f",&l);
    printf("Enter the value of width: ");
    scanf("%f",&b);
    
    area=l*b;
    printf("Area of the recatngle is :%f ",area);
}
